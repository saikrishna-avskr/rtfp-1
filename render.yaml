services:
  - type: web
    name: pharmassit
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn pharmassit.wsgi:application"
    envVars:
      - fromDotEnv: true
